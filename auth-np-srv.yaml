apiVersion: v1
kind: Service
metadata:
  name: authnpservice-srv
spec:
  type: NodePort
  selector:
    app: authsrv
  ports:
    - name: authsrv
      protocol: TCP
      port: 80
      targetPort: 80
